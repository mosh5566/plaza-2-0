# Fly.io — פריסה
# 1. flyctl auth login (פעם אחת)
# 2. flyctl launch  (בפעם הראשונה — ייצור אפליקציה)
# 3. flyctl deploy  (בכל פריסה)

app = "plaza-app"
primary_region = "fra"   # פרנקפורט (קרוב לישראל). אפשר "tlv" אבל פחות נפוץ.
kill_signal = "SIGTERM"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  PORT = "3000"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[http_service.checks]]
  grace_period = "10s"
  interval = "30s"
  method = "GET"
  timeout = "5s"
  path = "/api/health"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256

# Volume לקבצי DB ומדיה (persistent)
[mounts]
  source = "plaza_data"
  destination = "/app/db"
